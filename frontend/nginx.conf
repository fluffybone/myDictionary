server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Эта строка перенаправляет все запросы на index.html
        # Это критически важно для React Router
        try_files $uri $uri/ /index.html;
    }
}